#include <stdio.h>

int main() {
    // to print the average of three number
    float a,b,c;
    printf("enter three number ");
    scanf("%f %f %f",&a,&b,&c);
    printf("average of three number=%f",(a+b+c)/3);

    return 0;
}
